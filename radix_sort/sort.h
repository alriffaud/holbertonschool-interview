#ifndef SORT_H
#define SORT_H

#include <stdlib.h>
#include <stdio.h>

/* Prototype for the provided function that prints an array */
void print_array(const int *array, size_t size);

/* Prototype for the radix sort function */
void radix_sort(int *array, size_t size);

#endif /* SORT_H */
